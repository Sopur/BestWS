/**
 * All the TypedArray's BestWS encoder supports
 */

module.exports = [
    Int8Array,
    Int16Array,
    Int32Array,
    BigInt64Array,
    Uint8Array,
    Uint16Array,
    Uint32Array,
    BigUint64Array,
    Float32Array,
    Float64Array,
    Uint8ClampedArray,
];
